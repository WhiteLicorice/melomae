[project]
name = "melomae-tools"
version = "0.1.0"
description = "Model export and build tooling for Melomae."
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.uv]
package = false
